<div class="tabs-container">
    <div class="tabs-header">
      <button class="tab-button selected" data-tab="tab1">Tab 1</button>
      <button class="tab-button" data-tab="tab2">Tab 2</button>
      <button class="tab-button" data-tab="tab3">Tab 3</button>
    </div>
    <div class="tabs-content">
      <div class="tab-panel active" id="tab1">
        <h3 class="tab-title">Contenido del Tab 1</h3>
        <p>Este es el contenido del primer tab. Está visible por defecto.</p>
      </div>
      <div class="tab-panel" id="tab2">
        <h3 class="tab-title">Contenido del Tab 2</h3>
        <p>Este es el contenido del segundo tab. Solo se muestra cuando se selecciona.</p>
      </div>
      <div class="tab-panel" id="tab3">
        <h3 class="tab-title">Contenido del Tab 3</h3>
        <p>Este es el contenido del tercer tab. Solo se muestra cuando se selecciona.</p>
      </div>
    </div>
  </div>
  
  <style>
    .tabs-container {
      width: 100%;
      max-width: 768px;
      margin: 0 auto;
    }
    
    .tabs-header {
      display: flex;
      border-bottom: 1px solid #e5e7eb;
    }
    
    .tab-button {
      padding: 0.5rem 1rem;
      font-size: 0.875rem;
      font-weight: 500;
      color: #4b5563;
      background-color: #ffffff;
      border: none;
      border-bottom: 2px solid transparent;
      transition: all 0.2s ease-in-out;
      cursor: pointer;
    }
    
    .tab-button:hover {
      color: #1f2937;
      border-bottom-color: #d1d5db;
    }
    
    .tab-button.selected {
      color: #2563eb;
      border-bottom-color: #3b82f6;
    }
    
    .tab-button.selected:hover {
      color: #1d4ed8;
      border-bottom-color: #2563eb;
    }
    
    .tabs-content {
      padding: 1rem;
      background-color: #ffffff;
      border: 1px solid #e5e7eb;
      border-top: none;
      border-bottom-left-radius: 0.5rem;
      border-bottom-right-radius: 0.5rem;
    }
    
    .tab-panel {
      display: none;
    }
    
    .tab-panel.active {
      display: block;
    }
    
    .tab-title {
      font-size: 1.125rem;
      font-weight: 700;
      margin-top: 0;
      margin-bottom: 0.5rem;
    }
  </style>
  
  <script>
    // Esperar a que el DOM esté completamente cargado
    document.addEventListener('DOMContentLoaded', () => {
      const tabButtons = document.querySelectorAll('.tab-button');
      const tabPanels = document.querySelectorAll('.tab-panel');
      
      // Función para cambiar de tab
      function switchTab(tabId: string) {
        // Desactivar todos los tabs
        tabButtons.forEach(button => {
          button.classList.remove('selected');
        });
        
        tabPanels.forEach(panel => {
          panel.classList.remove('active');
        });
        
        // Activar el tab seleccionado
        const selectedButton = document.querySelector(`[data-tab="${tabId}"]`);
        const selectedPanel = document.getElementById(tabId);
        
        if (selectedButton && selectedPanel) {
          selectedButton.classList.add('selected');
          selectedPanel.classList.add('active');
        }
      }
      
      // Agregar eventos click a los botones de tab
      tabButtons.forEach(button => {
        button.addEventListener('click', () => {
          const tabId = button.getAttribute('data-tab');
          if (tabId) {
            switchTab(tabId);
          }
        });
      });
    });
  </script>